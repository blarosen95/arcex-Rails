<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional" "https://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" lang="en" xml:lang="en">
  <head>
    <title><%= @title %></title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <meta name="robots" content="noindex, nofollow">
    <meta name="googlebot" content="noindex, nofollow, noarchive">
    <%= stylesheet_link_tag "mailer_v5", media: "all" %>
  </head>
  <body>
    <div class="container w-100">
      <div class="body-content">
        <div class="card mb-4 card-background" >
          <div class="text-dark card-body pl-1 pr-1 pl-lg-2 pr-lg-2">
            <% if content_for?(:body_title) %>
              <h5 class="text-lg font-medium my-3"><strong><%= yield :body_title %></strong></h5>
            <% end %>
            <% if content_for?(:body_message).present? %>
              <p class="my-3 body-message tls tcl"><%= (yield :body_message).strip.html_safe %></p>
            <% end %>
            <div class="mt-3 text-center">
              <%= yield :body %>
              <% if @custom_email_footer_content %>
                <div class="mt-3">
                  <%= @custom_email_footer_content.html_safe %>
                </div>
              <% end %>
            </div>
            <% if content_for?(:sub_body) %>
              <div class="my-3 sub-body-content">
                <%= yield :sub_body %>
              </div>
            <% end %>
          </div>
        </div>
        <div class="footer text-muted">
          <% if content_for?(:footer) %>
            <div class="my-3">
              <%= yield :footer %>
            </div>
          <% end %>
          <div class="text-center" style='font-size:12px;'>
            Powered by <a href="https://arcex.com">ArcEx.com</a> -
            The dating app designed to never be deleted.
          </div>
          <div class="text-center" style='font-size:12px;'>
            Copyright &copy; <%= Time.now.year %> ArcEx LLC
          </div>
        </div>
        <div class="s-5"></div>
        <%= yield :scripts %>
      </div>
    </div>
  </body>
</html>
